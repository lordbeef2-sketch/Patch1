# Split Patch1 package

This package was rebuilt from the uploaded `Patch1.zip` as the base source.

It contains two independent patchers:

- `langflow/patcher`
- `open-webui/patcher`

Each one assumes the target install/environment is **one level up** from its own `patcher` folder.
